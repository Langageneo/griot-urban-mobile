dependencies:
  flutter:
    sdk: flutter
  # EXISTANTES (à garder)
  http: ^0.13.5
  flutter_svg: ^2.0.7
  cached_network_image: ^3.3.0
  flutter_dotenv: ^5.1.0
  intl: ^0.18.1
  shared_preferences: ^2.2.2
  connectivity_plus: ^5.0.2
  flutter_spinkit: ^5.1.0
  # NOUVELLE : Pour les paiements
  flutterwave: ^0.0.5
